#include "main.h"
/**
 * _strlen -returns length ofa string
 *
 *
 * Return - always 0
 */
int _string(char *s)
{
	char *s;
	int len;

	str = "cuea";
	len _strlen(str);
	*str = len;
	putchar("%d\n",*s);
	return(0);
}
