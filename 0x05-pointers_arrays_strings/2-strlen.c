#include "main.h"
/**
 * _strlen -returns length ofa string
 * @s: pointer to the string to check
 * Return - always 0
 */
int _strlen(char *s)
{
	char *s;
	int len;

	*s = "cuea";
	len = _strlen(str);
	_putchar("%d\n",*s);
	return(0);
}
