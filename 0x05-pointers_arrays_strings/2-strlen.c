#include "main.h"
/**
 * _strlen -returns length ofa string
 *
 *
 * Return - always 0
 */
int _string(char *s)
{
	char *s;
	int len;

	*s = "cuea";
	len = _strlen(str);
	_putchar("%d\n",*s);
	return(0);
}
